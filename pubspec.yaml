name: nutrilink
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  fl_chart: ^0.68.0  # Untuk Pie chart dan Bar chart
  intl: ^0.19.0     # Untuk format tanggal (Senin, 15 April 2025)

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
